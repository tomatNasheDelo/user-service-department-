 
 server:
      port: 8081
 
 spring:
 
    datasource:
        driver-class-name: org.postgresql.Driver
        password: postgres
        url: jdbc:postgresql://localhost:5432/employee_db
        username: karim
    jpa:
        hibernate:
            ddl-auto: create
            
 